export enum UserFields {
    ID = "id",
    CREATED_AT = "createdAt",
    UPDATED_AT = "updatedAt",
    USERNAME = "username",
    EMAIL = "email",
    PHONE_NUMBER = "phoneNumber",
    ROLE = "role",
}

export enum TablesNames {
    USERS = "users",
}

export type AllTablesColumns = UserFields;
